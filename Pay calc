hrs = input("Enter Hours: ")
h = float(hrs)
rate = input("Enter Rate: ")
r = float(rate)


if h > 40:
    pay = h * r
    ot = (h - 40) * (r * 0.5)
    xp = pay + ot
else:
    xp = h * r
print("Pay:",xp)
